-- ==========================================================
-- SQL DUMP UNTUK MODUL PENGGAJIAN PRESENSI ARJUNA
-- Database: u916236352_presensiarjuna
-- ==========================================================

CREATE TABLE IF NOT EXISTS `gaji_master` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `nik` VARCHAR(30) NOT NULL,
  `gaji_pokok` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `tunjangan_transportasi` DECIMAL(12,2) NOT NULL DEFAULT 150000.00,
  `tunjangan_jabatan` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `tunjangan_konsumsi` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `tunjangan_kehadiran` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `tunjangan_beras` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `tunjangan_fungsional` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `tunjangan_wali_kelas` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `tunjangan_masa_kerja` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `tarif_honor_kegiatan` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `tarif_ekskul` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `tarif_lembur` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `bpjs_kesehatan` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `bpjs_ketenagakerjaan` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `idx_gaji_master_nik` (`nik`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `penggajian_periode` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `kode_periode` VARCHAR(50) NOT NULL,
  `nama_periode` VARCHAR(100) NOT NULL,
  `bulan` INT NOT NULL,
  `tahun` INT NOT NULL,
  `tgl_mulai` DATE NOT NULL,
  `tgl_selesai` DATE NOT NULL,
  `hk_standar` INT NOT NULL DEFAULT 26,
  `total_siswa_tpa` INT NOT NULL DEFAULT 0,
  `status` ENUM('draft', 'final', 'terbayar') NOT NULL DEFAULT 'draft',
  `keterangan` TEXT NULL,
  `created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `idx_periode_kode` (`kode_periode`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `penggajian_detail` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `periode_id` BIGINT UNSIGNED NOT NULL,
  `nik` VARCHAR(30) NOT NULL,
  `kode_cabang` VARCHAR(20) NULL,
  `kode_dept` VARCHAR(20) NULL,
  `jabatan` VARCHAR(100) NULL,
  `hk_standar` INT NOT NULL DEFAULT 26,
  `hadir` INT NOT NULL DEFAULT 0,
  `izin` INT NOT NULL DEFAULT 0,
  `sakit` INT NOT NULL DEFAULT 0,
  `alpha` INT NOT NULL DEFAULT 0,
  `terlambat_jam` DECIMAL(6,2) NOT NULL DEFAULT 0.00,
  `gaji_pokok` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `tunjangan_transportasi` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `tunjangan_jabatan` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `tunjangan_konsumsi` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `tunjangan_kehadiran` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `tunjangan_lainnya` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `honor_kegiatan` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `honor_ekskul` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `upah_lembur` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `insentif_pool_tpa` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `reward_disiplin` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `bonus_tambahan` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `potongan_absen` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `potongan_terlambat` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `potongan_kasbon` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `potongan_bpjs` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `potongan_lainnya` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `total_penghasilan` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `total_potongan` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `gaji_bersih` DECIMAL(12,2) NOT NULL DEFAULT 0.00,
  `catatan` TEXT NULL,
  `status_kirim_wa` TINYINT(1) NOT NULL DEFAULT 0,
  `waktu_kirim_wa` DATETIME NULL,
  `pdf_path` VARCHAR(255) NULL,
  `created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  INDEX `idx_detail_periode` (`periode_id`),
  INDEX `idx_detail_nik` (`nik`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
